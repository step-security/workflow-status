name: "Workflow Status Action"
description: "Trigger events like notifications or alerts using the workflow status"
author: "step-security"

outputs:
  status:
    description: "Returns either success or failed as workflow status"
    value: ${{ steps.workflow-status.outputs.status }}
runs:
  using: "composite"
  steps:
     - name: Subscription check
       env:
         REPO_PRIVATE: ${{ github.event.repository.private }}
       run: |
         UPSTREAM="martialonline/workflow-status"
         ACTION_REPO="${GITHUB_ACTION_REPOSITORY:-}"
         DOCS_URL="https://docs.stepsecurity.io/actions/stepsecurity-maintained-actions"

         echo ""
         echo -e "\033[1;36mStepSecurity Maintained Action\033[0m"
         echo "Secure drop-in replacement for $UPSTREAM"
         if [ "$REPO_PRIVATE" = "false" ]; then
           echo -e "\033[32m✓ Free for public repositories\033[0m"
         fi
         echo -e "\033[36mLearn more:\033[0m $DOCS_URL"
         echo ""

         if [ "$REPO_PRIVATE" != "false" ]; then
           SERVER_URL="${GITHUB_SERVER_URL:-https://github.com}"

           if [ "$SERVER_URL" != "https://github.com" ]; then
             BODY=$(printf '{"action":"%s","ghes_server":"%s"}' "$ACTION_REPO" "$SERVER_URL")
           else
             BODY=$(printf '{"action":"%s"}' "$ACTION_REPO")
           fi

           API_URL="https://agent.api.stepsecurity.io/v1/github/$GITHUB_REPOSITORY/actions/maintained-actions-subscription"

           RESPONSE=$(curl --max-time 3 -s -w "%{http_code}" \
             -X POST \
             -H "Content-Type: application/json" \
             -d "$BODY" \
             "$API_URL" -o /dev/null) && CURL_EXIT_CODE=0 || CURL_EXIT_CODE=$?

           if [ $CURL_EXIT_CODE -ne 0 ]; then
             echo "Timeout or API not reachable. Continuing to next step."
           elif [ "$RESPONSE" = "403" ]; then
             echo -e "::error::\033[1;31mThis action requires a StepSecurity subscription for private repositories.\033[0m"
             echo -e "::error::\033[31mLearn how to enable a subscription: $DOCS_URL\033[0m"
             exit 1
           fi
         fi
       shell: bash

     - name: Check status
       id: workflow-status
       run: |
        url="${GITHUB_API_URL:-https://api.github.com}/repos"
        repo="${GITHUB_REPOSITORY}"
        run_id="${GITHUB_RUN_ID}"
        token="${{ github.token }}"

        failure=$(curl -sS --retry 5 --retry-delay 2 --connect-timeout 10 \
          -H "Authorization: token ${token}" \
          "${url}/${repo}/actions/runs/${run_id}/jobs?per_page=100" | \
        jq -r '.jobs[] | select(.status == "completed" and .conclusion == "failure").conclusion' | \
        wc -l)

        cancelled=$(curl -sS --retry 5 --retry-delay 2 --connect-timeout 10 \
          -H "Authorization: token ${token}" \
          "${url}/${repo}/actions/runs/${run_id}/jobs?per_page=100" | \
        jq -r '.jobs[] | select(.status == "completed" and .conclusion == "cancelled").conclusion' | \
        wc -l)

        if [ "${failure}" -gt 0 ]; then
          status="failure"
        elif [ "${cancelled}" -gt 0 ]; then
          status="cancelled"
        else 
          status="success"
        fi

        echo "status=${status}" >> $GITHUB_OUTPUT
       shell: bash
branding:
  icon: check-square
  color: gray-dark
